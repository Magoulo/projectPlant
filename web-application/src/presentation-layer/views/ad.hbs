
<!-- Lägg till
1. Title (och latin name under)längst upp till vänster
2. Bilder under title (cover image som först)
3. Description till höger
4. En "like" button för annonsen (Vart IDk?)
-->

<div class="content">
<h1>Ad</h1>

{{#if errors.length}}
	<p>Oh no, something went wrong:</p>
	<ul>
		{{#each errors}}
			<li>{{this}}</li>
		{{/each}}
	</ul>
{{else}}
	
	{{#if ad}}
        <ul>		
			<dt>Title:</dt>
			<dd>{{ad.title}}</dd>

			<dt>LatinName:</dt>
			<dd>{{ad.latinName}}</dd>

			<dt>description:</dt>
			<dd>{{ad.description}}</dd>

			 <li><img src="{{ad.coverImagePath}}" alt="{{imageBundleID}}" class="flowerImage" style="width: 300px; height: 300px">
			 <img src="{{ad.firstImagePath}}" alt="{{imageBundleID}}" class="flowerImage" style="width: 300px; height: 300px">
			<img src="{{ad.secondImagePath}}" alt="{{imageBundleID}}" class="flowerImage" style="width: 300px; height: 300px"></li>
        </ul>
        <button>Make a bid</button>
        <button>Mail owner {{user.email}}</button>
        
		
	{{else}}
		<p>No, that ad doesn't exist here.</p>
	{{/if}}
	
{{/if}}
</div>